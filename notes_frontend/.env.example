# Public base URL for the backend API that serves notes.
# Example values:
#   http://localhost:4000    -> if backend runs on 4000 and CORS allows this origin
#   /api                     -> if you proxy API via the same origin
NUXT_PUBLIC_API_BASE=http://localhost:4000
